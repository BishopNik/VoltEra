{
  "name": "ink-energy-platform",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "build": "npm run generate:sitemap",
    "generate:sitemap": "node scripts/generate-sitemap.mjs",
    "start": "node server.mjs",
    "dev": "node --watch server.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "mongodb": "^6.17.0"
  }
}
